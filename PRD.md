# 体重管理 App(BodyMetrics)PRD

> 本文档为 AI agent 实现用产品需求文档。视觉与交互规格以 Figma Make 生成的原型(`.figma/make` Web 项目)为准,本文档只定义原型中无法体现的产品逻辑、数据模型与平台行为。**一次交付,不分阶段。**

## 1. 概述

- **产品**:记录每日体重(可选体脂率)的极简体重管理 App,界面风格为 iOS 原生(iOS 26+,SwiftUI,含原生 Liquid Glass)。
- **显示名**:衡(可在 Xcode target 的 Info.plist 配置;若用户另行指定则以其为准)。
- **核心原则**:纯本地、无账号、无网络。数据不出设备。
- **范围**:记录、日历、趋势、设置 4 个 tab + 记录弹层,全部一次实现。

## 2. 功能需求

### 2.1 全局框架

- 底部导航:采用 iOS 26 原生 `TabView`(系统默认 Liquid Glass 样式,贴底),4 个 tab:**记录 / 日历 / 趋势 / 设置**。
- 选中态:系统 Liquid Glass 高亮,跟随 app tint 主色 `#258257`。
- 注:原型中的“悬浮胶囊导航”为设计示意;以 iOS 26 系统原生液态玻璃 Tab Bar 为准,不自定义容器。
- 顶部状态栏区域照原型(9:41 + 灵动岛),系统级即可,不强求像素还原。

### 2.2 记录页(默认首页)

- 页头:小字日期(如"星期一,8月18日",按真实当天日期)、大标题"体重"、右上角圆形日历图标按钮(点击跳转"日历"tab)。
- **今日体重卡片**(绿色渐变 hero):
  - 有今日记录:显示"今日体重"、当日最新一条体重值(如 `62.4 kg`)、"↓ 较昨日 X.X kg"与记录时间(如 `08:16`)。
  - 无今日记录:显示"今日体重"与占位 `--`、文案"尚未记录",不显示较昨日。
  - "较昨日":今日最新体重 − 昨日最新体重;任一缺失则隐藏。
- **进度区**:
  - 目标体重行:显示目标值(默认 `58.0 kg`)、"距离 58.0 kg 还有 X.X kg",右侧进度环百分比(规则见 §5.3)。
  - 本周状态行:显示"趋势平稳/上升/下降"与近 7 日变化值(规则见 §5.4)。
- **最近记录**:按时间倒序列出全部记录,**同日多条分行显示**(如"今天 08:00 62.4 kg"、"今天 21:00 62.6 kg");超过 3 行截断,右侧"查看全部"按钮 → **跳转"日历"tab**(设计缺口,默认行为见 §8)。
- 底部主按钮"记录体重"→ 打开记录弹层。

### 2.3 记录弹层

- 底部弹出 sheet,标题"记录体重"。
- **指标切换**(设计扩展,见 §8):弹层顶部"体重 / 体脂率"分段控件,切换后标题、单位、键盘输入逻辑联动。
- 说明文案:"输入体重,可输入 1 位小数,如 62.4 kg"(体脂率时:"如 18.6 %")。
- **输入逻辑**:使用系统原生 `.decimalPad` 键盘(自带小数点、布局与辅助功能);**不限制整数位数**,小数部分最多 1 位(如 `62.4`、`60`、`18.6`),输入时过滤非法字符;键盘上方通过 SwiftUI keyboard toolbar 加"收起键盘"按钮;底部"完成"按钮,输入为空时禁用。
- **保存**:追加写入一条记录(体重、体脂率各为独立记录);**允许同日同指标多条**(如早晚各一次),不覆盖历史数据。保存后关闭弹层,记录页 hero 与最近记录即时刷新。
- 弹层以下滑/取消按钮关闭(不做"点背景关闭"手势);未保存不落库。

### 2.4 日历页

- 月视图:年份月份标题(如"2026年8月")、左右切换月份按钮、周标题(日一…六,周日红、周六蓝照原型)。
- 每个日期格显示当日最新一次体重值(小字);今天高亮(绿色描边 + 绿色日期);非当月日期置灰。
- 底部当日详情条:日期 + "今天"标记、当日体重值、右侧"编辑"按钮。
- **编辑**:点击后打开记录弹层,预填该日最新一条的数值,保存后作为新记录追加(不覆盖历史)。
- 空数据:日期格无值留空,不显示占位符。

### 2.5 趋势页

- 页头居中标题"趋势"。
- 指标分段:"体重 / 体脂率 / BMI"(切换图表数据源)。
- 范围分段:"1周 / 1个月 / 3个月"(切换时间窗口)。
- 大数值区:当前窗口内最新值 + 单位,右侧趋势值(如 `↓ 0.6`)。
- **折线图**:Swift Charts 绘制所选指标在窗口内的每日值(取当日最新一条);体重/BMI 保留 1 位小数,体脂率保留 1 位小数带 `%`。
- 横轴日期标签(起点 / 中点 / 今天)。
- 窗口内无数据:图表区显示空态"暂无数据"。
- **趋势洞察**卡片:文案与数值规则见 §5.5。

### 2.6 设置页

- **记录 分组**:
  - 体重单位:展示"千克(kg)"(v1 仅展示,不可切换,或切换后仅影响显示,默认 kg)。
  - 我的目标:可编辑的目标体重,默认 `58.0 kg`。
  - 身高(设计扩展,支撑 BMI,见 §8):可编辑,默认 `170 cm`,用于 §5.2 BMI 计算。
- **提醒 分组**:
  - 记录提醒:开关,默认开启;开启时每天 `08:00` 发送本地通知"该记录体重啦";首次开启请求通知权限,被拒时提示去系统设置开启。
- **数据与隐私 分组**:
  - 导出 CSV:导出全部体重记录为 CSV 文件(`衡-体重记录.csv`,UTF-8 with BOM,列:日期,体重(kg)),通过 ShareSheet 分享;成功后展示 toast"已导出「衡-体重记录.csv」"(自定义 toast 为产品风格选择,非系统组件,保留)。
  - 面容 ID / 触控 ID 解锁:开关,默认关。开启后:App 进入后台立即锁定,回到前台需生物识别验证(`LocalAuthentication` 的 `.deviceOwnerAuthentication` 策略,失败可回退系统密码);可在 App 内关闭;用户也可在系统「设置 → 面容 ID 与密码 → 其他 App」单独关闭本 App 权限,此时 App 内开关自动失效并提示去系统设置开启。

### 2.7 辅助功能(无障碍,强制)

- **VoiceOver**:所有交互元素提供可读标签(图标按钮有 `accessibilityLabel`);保存等动态变化通过 `accessibilityAnnouncement` 播报。
- **Dynamic Type**:全部文本使用系统字号,随系统字体缩放自适应,不截断、不错位。
- **点击目标**:可点击区域不小于 44×44pt。
- **对比度**:正文与背景对比度满足 HIG 要求;Color Set 的 Dark 外观取值同样校验。

## 3. 数据模型(SwiftData)

```swift
enum Metric: String, Codable { case weight, bodyFat }

@Model
final class MetricEntry {
  var id: UUID
  var metric: Metric        // 体重 / 体脂率
  var value: Double         // 数值,单位 kg 或 %
  var date: Date            // 记录日期(日历日,含时分秒)
}

@Model
final class UserProfile {
  var goalWeight: Double    // 目标体重,默认 58.0
  var heightCm: Double      // 身高,默认 170.0
  var reminderEnabled: Bool // 提醒开关,默认 true
  var biometricLockEnabled: Bool // 面容 ID / 触控 ID 解锁开关,默认 false
}
```

- 追加式:每条记录独立存储,**允许同日同指标多条**;查询按 `date` 排序,"当日代表值"取当日最新一条。
- 生物识别策略由系统管理(`LocalAuthentication`),App 不存储任何密码。
- 全部数据纯本地,不申请任何网络权限。

## 4. 计算规则

| 项 | 规则 |
|---|---|
| 今日体重 | 当日 `metric == .weight` 的最新一条记录 |
| 较昨日 | 今日最新体重 − 昨日最新体重;任一缺失则隐藏 |
| 体重单位显示 | 输入允许 0–1 位小数,显示统一保留 1 位小数 + `kg`;体脂率同规则 + `%` |
| BMI | 当日体重 ÷ (身高 m)²,保留 1 位小数;无当日体重则无当日 BMI |
| 目标进度 | `(首个体重记录 − 当前体重) / (首个体重记录 − 目标体重)`,截断到 0%–100%;不足 2 条记录或差为 0 时显示 0% |
| 本周状态 | 本周体重均值 − 上周体重均值;|Δ| ≤ 0.3 kg 文案"趋势平稳",否则"上升/下降" + 带符号数值 |
| 趋势洞察 | 窗口首日 vs 窗口最后一日体重差,近 30 天窗口;|Δ| ≤ 0.5 kg"保持平稳",否则"稳定向下/上升" + "近 30 天已减少/增加 X.X kg" |

## 5. 平台与技术方案

- SwiftUI + SwiftData,iOS 26.0+(SwiftData 自 iOS 17 即可用,无冲突)。
- 底部导航:iOS 26 原生 `TabView` Liquid Glass,不自定义。
- 趋势图:Swift Charts。
- 提醒:本地通知 `UNUserNotificationCenter`,`UNCalendarNotificationTrigger` 每天 08:00。
- 隐私锁定:`LocalAuthentication`(`.deviceOwnerAuthentication`,生物识别失败回退系统密码);ScenePhase 进入 background 即锁定,回到 active 且开关开启时验证;用户可在系统「设置 → 面容 ID 与密码」关闭本 App 权限。
- CSV:导出文件到临时目录后 ShareSheet 分享。
- 不引入任何第三方依赖、不接入网络、不收集任何分析数据。
- 颜色统一通过 `Assets.xcassets` 的 Color Set 管理(语义命名),代码禁止硬编码颜色值(见 §6)。
- 文案统一通过 **String Catalog**(`.xcstrings`)管理,代码用 `String(localized:)` / `LocalizedStringKey` 引用,**禁止在代码中硬编码用户可见文案字面量**(与颜色同款原则);顺带获得免费本地化能力。

## 6. 视觉规格

以原型代码(`src/index.css`、`src/App.tsx`)为准,要点:

- **颜色集中管理(强制)**:所有颜色定义在 `Assets.xcassets` 的 Color Set(语义化命名),代码用 `Color("Name")` / `UIColor(named:)` 引用;**禁止在 Swift 代码中硬编码 hex、`Color(red:green:blue:)` 或 `UIColor(red:...)` 字面量**。原型中的色值只作为 Color Set 的取值参考:

  | Color Set 命名 | 取值(浅色) | 用途 |
  |---|---|---|
  | `BrandGreen` | `#258257` | 主按钮、强调、tab tint |
  | `HeroGradientStart` | `#246347` | 今日卡片渐变起点 |
  | `HeroGradientEnd` | `#3f9b68` | 今日卡片渐变终点 |
  | `PageBackground` | `#dfe7e1` | 页面背景 |
  | `CardBackground` | 白色 80% 不透明度 | 列表卡背景 |
  | `TextPrimary` | `#17231e` | 主文字 |
  | `TextSecondary` | `#6e7e74` | 次要文字/说明 |

- 数字使用等宽字体风格(DM Mono 等价物,系统 `monospacedDigit` 即可);正文系统字体。
- 圆角列表卡、iOS 风格开关,均照原型;底部导航用系统原生 Liquid Glass,不自定义。
- 深浅色:适配深色模式;§6 所有 Color Set 需提供 Dark 外观取值,深浅色下均校验对比度与可读性。

## 7. 验收标准

1. 冷启动进入记录页,无数据时今日卡片显示占位,不崩溃。
2. 记录弹层可输入 `60`、`62.4` 等任意整数位数(1 位小数),保存后今日卡片、最近记录、日历格即时更新;**同日同指标可追加多条**(如早晚各一次),历史记录不被覆盖;整数位不受 3 位限制。
3. 体重与体脂率分别记录互不覆盖;趋势页三个指标切换图表数据正确。
4. 日历翻月正确、今天高亮、编辑预填正确且保存为追加(不覆盖历史)。
5. 趋势页 1周/1个月/3个月 窗口数据与数值正确,空窗显示空态。
6. 设置修改目标体重/身高后,记录页进度、趋势页 BMI 即时重算。
7. 提醒开启后系统能收到每天 08:00 本地通知;权限被拒有引导。
8. 导出 CSV 文件可用 Excel/Numbers 打开且中文不乱码;内容与记录一致。
9. 开启面容 ID / 触控 ID 后,切后台立即锁定、回前台需验证;系统设置关闭本 App 权限后,App 内开关显示不可用并引导去系统设置开启。
10. App 飞行模式下全功能可用(纯本地验证)。
11. 代码中不存在硬编码颜色字面量(hex / `Color(red:green:blue:)` / `UIColor(red:...)`);所有颜色来自 `Assets.xcassets` 的 Color Set(含 Dark 外观取值),以 `Color("...")` 引用。
12. 辅助功能:VoiceOver 可朗读全部操作元素;Dynamic Type 放大后不截断;点击区域 ≥44pt。
13. 代码中不存在硬编码的用户可见文案字面量;所有文案来自 String Catalog(`.xcstrings`),以 `String(localized:)` 引用。

## 8. 设计缺口与默认决策(可改)

| 缺口 | 默认决策 | 备注 |
|---|---|---|
| 趋势页有"体脂率",记录页无入口 | 记录弹层顶部加"体重/体脂率"分段切换 | 用户已确认 |
| BMI 需要身高 | 设置页新增"身高"项,默认 170 cm | 最小补充 |
| 首页"查看全部"无对应页面 | 跳转"日历"tab | 如需独立列表页再议 |
| App 显示名 | 暂定"衡"(与设计稿 CSV 文件名一致) | 可改 |
| HealthKit 集成 | **暂不做**(用户已确认);留作后续扩展:同步体重/体脂率到系统健康 App | 手输为主,暂不引入权限弹窗与双写复杂度 |
